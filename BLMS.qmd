---
title: "Buffalo Lake Métis Settlement"
subtitle: "Results from the Cumulative Effects Wildlife Camera Project"
format:
  html:
    grid:
      margin-width: 200px
      body-width: 950px
navbar: right
theme: cosmo
date: "`r format(Sys.time(), '%d %B, %Y')`"
author:
  - name: "Timothy Patenaude"
    affiliation: "Buffalo Lake Métis Settlement"
  - name: "David Evans"
    affiliation: "ABMI"
    email: "david.evans@ualberta.ca"
  - name: "Robbie Potts"
    email: "rnpotts@ualberta.ca"
    affiliation: "ABMI"
  - name: "Marcus Becker"
    affiliation: "ABMI"
    email: "marcus.becker@ualberta.ca"
editor: visual
bibliography: references.bib
nocite: '@*'
toc: true
toc-depth: 3
toc-expand: true
toc-location: left
css: assets/styles.css
---

<br>

```{r}
#| warning: false
#| echo: false
source("Functions/Carousel.R")
carousel("gallery-carousel", 10000, 
         yaml.load_file("Images/BLMS/blms-carousel1.yml"))
```

::: {.callout-note collapse="true" style="background-color: #f4f4f4; padding: 20px;"}
This report is dynamically generated, meaning its results may evolve with the addition of new data or further analyses. For the most recent updates, refer to the publication date and feel free to reach out to the authors.
:::

# Project Introduction

```{r}
#| label: load-all
#| include: false
#| echo: false
#| eval: true
#| warning: false
#| message: false

# Load packages
library(tidyverse)
library(leaflet)
library(leaflet.extras)
library(sf)
library(activity)
library(overlap)
library(corrplot)
library(DT)
library(here)
library(htmltools)
library(htmlwidgets)
library(plotly)
library(terra)
library(glue)

# Community acronym
com_acr <- "BLMS"

# Community full name
com_full <- "Buffalo Lake Métis Settlement"

# Google Drive
g_drive_cbme <- "G:/Shared drives/CBME Community Camera Results/"

# Load data from the community (including pre-processed data)
load(paste0(g_drive_cbme, com_acr, "/Data/", com_acr, " Data Objects.RData"))

species <- c(
  "White-tailed Deer", 
  "Black Bear", 
  "Moose", 
  "Coyote", 
  "Snowshoe Hare",
  "Feral Horse")

species_colours <- c(
  "White-tailed Deer"  = "#3A86FF",  
  "Snowshoe Hare"      = "#E63946",  
  "Moose"              = "#06D6A0",  
  "Black Bear"         = "#8338EC",  
  "Feral Horse"        = "#FB5607",  
  "Coyote"             = "#3366CC"  
)

```

The Buffalo Lake Wildlife Camera Program (BLWCP) is a community-led initiative grounded in Indigenous knowledge and stewardship values to understand how wildlife, particularly moose, supports Métis culture and way of life in northeastern Alberta. Moose are central to the `r com_full` (`r com_acr`) as a vital food source and cultural symbol. In response to growing concerns over oil and gas development, `r com_acr` partnered with the Alberta Biodiversity Monitoring Institute (ABMI) to implement a camera-based wildlife monitoring program that compares two regions: one protected for wildlife conservation and one facing potential industrial development.

`r com_acr` began this camera program in 2024 and deployed 41 cameras between April and November of 2024. Cameras were deployed in 2 grids of cameras focusing on two areas of interest to the Settlement: Rubellite West and Goose Lake. Rubellite West is to the West of the Settlement and is slated for potential oil and gas development. The Goose Lake area is within the Settlement and is a wildlife protection area. The goal of the camera program was to compare wildlife abundance between both important areas, as well as establish a baseline of wildlife abundance prior to potential development in the Rubellite West area.

## Camera Refresh and Image Processing

The wildlife cameras were refreshed (given new SD cards and batteries) in the field by the BLMS field crew in August 2025. BLMS community members uploaded and tagged all of the camera images on the online platform [WildTrax](www.wildtrax.ca)[^1]. WildTrax is an online platform developed by the [Alberta Biodiversity Monitoring Institute](www.abmi.ca) for users of environmental sensors such as remote cameras to provide support with processing, organizing, and sharing data. Initial results from this first year of data collected at the 40 cameras are presented here in this report.

[^1]: `r com_acr` images and wildlife tags can be viewed using [this link](https://portal.wildtrax.ca/home/camera-deployments.html?sensorId=CAM&projectId=3150) to the WildTrax platform. Note that viewers will need to have a WildTrax account with the appropriate permissions from the community and be logged in to see the images.

# Project Highlights

This section is a space for BLMS staff and community members to share their major takeaways from the project so far. This could be sharing surprises from the images, camera deployment/refresh, potential to add quotes, or create videos and add them in! Highlight specific images, etc.

One idea we came up with was: One highlight of the program has been seeing how abundant the moose are in the Rubelite west camera grid area!

# Camera Locations

The following interactive map can be used to explore the locations of the cameras deployed by `r com_full`. Satellite imagery can be switched on and off using the button in the top left of the top.

Click each camera location to see it's name, grid, and when it was deployed and the SD cards were retrieved.

<br>

```{r}
#| label: map-camera-locations
#| include: true
#| echo: false
#| results: asis
#| eval: true
#| warning: false
#| message: false

cam <- makeAwesomeIcon(
  icon = "camera",
  iconColor = "black",
  library = "ion",
  markerColor = "white"
)

location_ranges <- df_od |>
  group_by(project_location) |>
  summarize(start_date = min(date), end_date = max(date), .groups = "drop") |>
  separate(project_location, into = c("project", "location"), sep = "_")

locations_for_map <- location_report |>
  mutate(grid = ifelse(str_detect(location, "^GL"), "Goose Lake", "Rubellite West")) |>
  left_join(location_ranges, by = "location")

buffer <- 0.05

htmltools::tags$div(
  style = "display: flex; justify-content: center;",
  
  leaflet(data = st_as_sf(locations_for_map, coords = c("longitude", "latitude"), crs = 4326), 
          width = "90%", height = "750px") |>
    addTiles() |>
    addProviderTiles("Esri.WorldImagery", group = "Satellite Imagery") |>
    fitBounds(
    lng1 = min(locations_for_map$longitude) - buffer,
    lat1 = min(locations_for_map$latitude) - buffer,
    lng2 = max(locations_for_map$longitude) + buffer,
    lat2 = max(locations_for_map$latitude) + buffer
  ) |>
    addFullscreenControl() |>
    addResetMapButton() |>
    addScaleBar(position = "bottomright", options = scaleBarOptions(imperial = FALSE)) |>
    addMiniMap(position = "bottomright") |>
    addAwesomeMarkers(
      icon = makeAwesomeIcon(icon = "camera", iconColor = "black", library = "ion", markerColor = "white"),
      group = "Camera Locations",
      popup = paste(
        "<b>", "Location: ", "</b>", locations_for_map$location, "<br><br>",
        "<b>", "Grid:", "</b>", locations_for_map$grid, "<br><br>",
        "<b>", "Deployed:", "</b>", locations_for_map$start_date, "<br><br>",
        "<b>", "Refreshed:", "</b>", locations_for_map$end_date, "<br><br>"
      )
    ) |>
    addLayersControl(
      overlayGroups = c("Satellite Imagery", "Camera Locations"),
      options = layersControlOptions(collapsed = FALSE),
      position = "topright"
    )
)
  
```

<br>

# Deployment and Retrieval Dates

@fig-ranges below displays when each camera location was deployed and when the SD cards were retrieved. Deployment took place in mid April 2024, and the majority of the cameras were serviced with new SD cards in early November 2024 after 6 months of operation. Two cameras (GLR6-SD36 and GLR12-SD14) failed earlier than the target service date, and two cameras (RW20-SD20 and RW19-SD17) were serviced at a later date.

<br>

```{r}
#| label: fig-ranges
#| fig-cap: "Camera deployment and SD card retrieval dates."
#| fig-cap-location: bottom
#| fig-dpi: 500
#| out-width: "85%"
#| include: true
#| echo: false
#| eval: true
#| warning: false
#| message: false

knitr::include_graphics(paste0("Figures/", com_acr, "/Deployment Ranges.png"))


```

# Number of Images Collected

@fig-nimages below displays the total number of images collected on the most common 6 species tagged in the `r com_full` camera project. The most commonly observed species was `r nimages[1, 1] |> pull()` with `r nimages[1, 2] |> pull()` images collected over the course of the project.

```{r}
#| label: fig-nimages
#| fig-cap: "Number of images captured by species"
#| fig-cap-location: bottom
#| fig-dpi: 300
#| out-width: "85%"
#| include: true
#| echo: false
#| eval: true
#| warning: false
#| message: false

knitr::include_graphics(paste0("Figures/", com_acr, "/Number of Images.png"))

```

For a complete list of all the species, you can go to the project on WildTrax and click on the 'Species' tab of the project. Since no other species had more than 100 images collected over the course of the project and across all cameras, we will focus our analysis on the 6 species shown here ([White-tailed deer](https://portal.wildtrax.ca/home/camera-deployments/species-verification.html?projectId=3150&speciesId=2699), [Black Bear](https://portal.wildtrax.ca/home/camera-deployments/species-verification.html?projectId=3150&speciesId=2635), [Moose](https://portal.wildtrax.ca/home/camera-deployments/species-verification.html?projectId=3150&speciesId=2678), [Coyote](https://portal.wildtrax.ca/home/camera-deployments/species-verification.html?projectId=3150&speciesId=2658), [Snowshoe Hare](https://portal.wildtrax.ca/home/camera-deployments/species-verification.html?projectId=3150&speciesId=2740), and [Feral Horse](https://portal.wildtrax.ca/home/camera-deployments/species-verification.html?projectId=3150&speciesId=2836)).

<br>

```{r}
#| warning: false
#| echo: false
#| include: true
#| eval: true
#| message: false

carousel("species-carousel", 10000, 
         yaml.load_file("Images/BLMS/blms-carousel2.yml"))
```

# Independent Detections Over Time

The number of images of each species tells us part of the picture but just because there may be more images of one species doesn’t necessarily mean there are more of them in the area, they may have just spent more time in front of the camera. With camera data, it’s important to track the number of “independent” detections for each species. You can think of an independent detection as an image or collection of images for a visit from an animal or group of animals.

In the figure below we present the number of independent detections for each of the 6 focal species detected in the `r com_acr` project. We use a 30 minute time interval to separate detection events; that is, images captured within 30 minutes of each other count toward a single detection and images with a gap of greater than 30 minutes between them count as multiple independent detections. Images that are captured of the same species over less than 30 minutes are assumed to be of the same individual, or group of individuals.

You can explore the figures for each species using the tabs below. The x-axis (bottom) represents the camera deployment timeline, from April 2024 to November 2024. The y-axis (left) shows the number of detections captured, from all of the cameras from the project. Note that the y-axis (left) of the plots for each species may change depending on the number of detections captured.

```{r}
#| label: ind-detections
#| echo: false
#| results: asis
#| include: true
#| eval: true
#| warning: false
#| message: false

file_names <- paste0("Independent Detections ", species, ".png")

figure_letters <- letters[1:length(species)]

cat("::: panel-tabset\n\n")

for (i in seq_along(species)) {
  title <- species[i]
  letter <- figure_letters[i]
  img_path <- here::here("Figures/BLMS", file_names[i])
  rel_path <- fs::path_rel(img_path, start = getwd())

  cat("### ", title, " {.unnumbered}\n\n", sep = "")
  
  # Output raw HTML as a single string
  html <- glue::glue(
    '<div style="text-align: center;">
       <img src="{rel_path}" style="width:80%;">
       <figcaption style="text-align: center; font-size: 0.95em; margin-top: 0.5em;">
         <strong>Figure 3{letter}.</strong> Independent detections of <strong>{title}</strong> across the study time period.
       </figcaption>
     </div>\n\n'
  )
  
  cat(html)
}

cat(":::\n")

```

-   **White-tailed deer** were primarily detected in the summer months of June and July. This coincides with when fawns are typically born, creating a "birth pulse" in the population.
-   Because they hibernate over the winter, detection of **Black Bears** began in May, and peaked in late June and early July.
-   Similar to deer, **Moose** detections peaked in mid-summer, particularly in July.
-   **Coyote** were detected year-round, with a notable peak in the late spring and early summer months of May and June.
-   Detections of **Snowshoe Hare** were consistent year-round with the exception of mid-summer where July and August recorded very few or no detections. This is possibly due to cameras better able to pick up the thermal signature of the species in winter.
-   **Feral Horses** were sporadically detected throughout the camera deployment period.

<br>

The table below summarises the number of images collected for each species and the number of independent detections.

<br>

```{r}
#| include: true
#| echo: false
#| eval: true
#| warning: false
#| message: false
#| results: asis

ndetect <- df_ind_detect |>
  group_by(species_common_name) |>
  tally(name = "detect") |>
  filter(species_common_name != "Unidentified")

joined_table <- nimages |>
  left_join(ndetect, by = "species_common_name")

htmltools::div(
  style = "display: flex; flex-direction: column; align-items: center;",

  htmltools::tagList(
    DT::datatable(
      joined_table,
      options = list(
        searching = TRUE,
        paging = TRUE,
        pageLength = 10,
        autoWidth = TRUE
      ),
      width = "70%",
      colnames = c("Species", "Number of Images", "Number of Detections")
    ),

    htmltools::tags$figcaption(
      style = "text-align: center; font-size: 1.1em; margin-top: 0.5em;",
      strong("Table 1."), " Number of images captured and independent detections by species in the camera project."
    )
  )
)

```

# Temporal Activity Patterns

Since camera traps take photos continuously 24 hours a day they can tell us when different species are active. These analyses can give insight into competition, predation, and coexistence.

In this section we present the temporal (diel) activity patterns for each of the 6 focal species from the `r com_acr` cameras. The x-axis (bottom) of each of the following plots represents the 24-hr daily cycle, and the line represents the relative activity level detected by the cameras. This data is pooled across all 40 cameras in the project.

```{r}
#| include: true
#| echo: false
#| eval: true
#| warning: false
#| message: false
#| results: asis

# Set folder containing the plots
file_names <- paste0("Activity ", species, ".png")
figure_letters <- letters[1:length(species)]

cat("::: panel-tabset\n\n")

for (i in seq_along(species)) {
  title <- species[i]
  letter <- figure_letters[i]
  img_path <- here::here("Figures/BLMS", file_names[i])
  rel_path <- fs::path_rel(img_path, start = getwd())
  
  cat("### ", title, " {.unnumbered}\n\n", sep = "")
  
  # Render the image and caption
  html <- glue::glue(
    '<div style="text-align: center;">
       <img src="{rel_path}" style="width:80%;">
       <figcaption style="text-align: center; font-size: 0.95em; margin-top: 0.5em;">
         <strong>Figure 4{letter}.</strong> Daily activity pattern of <strong>{title}</strong> based on camera trap detections.
       </figcaption>
     </div>\n\n'
  )
  
  cat(html)
}

cat(":::\n")


```

-   **White-tailed deer** are primarily active during twilight hours, specifically at dawn (6:00 to 9:00) and dusk (19:00 to 21:00). This pattern is known as *crepuscular*.
-   **Black Bear** showed the opposite pattern, with more of their activity being recorded during the day (and specifically before 12:00).
-   Based on the cameras in the `r com_acr` project, **Moose** were primarily active during the night time (3:00 to 6:00), with smaller peaks occurring throughout the day and into the evening.
-   **Coyote** were exclusively active in the early parts of the night, around 24:00.
-   **Snowshoe Hare** activity increased throughout the evening beginning at 18:00, carrying throughout the night until dawn. They were not active during the day.
-   Due to the fewer number of images captured, it is difficult to assess a pattern for **Feral Horses**.

# Species Overlap

Camera trap data are being increasingly used to model multiple species communities, and we can use this data to explore the co-occurrence patterns of the species in the community.

## Species that share the same spaces

Blue boxes in Figure 5 below show species that are usually seen by the same cameras. For example, Moose and White-tailed Deer often share the same space because the browse on similar vegetation.

## Species rarely found together

Red (or white) boxes show species that tend to stay apart and weren't usually seen at the same camera. For example, White-tailed Deer and Feral Horses don't seem to share the same habitat preferences. Similarly, deer often prefer upland habitats whereas Snowshoe Hare prefer lowland areas.

```{r}
#| label: cooccurrences
#| include: true
#| echo: false
#| eval: true
#| warning: false
#| message: false
#| fig-cap: "Figure 5. Co-Occurrence values for the 8 focal species."

par(mfrow=c(1,1))

corrplot(M, method = "color",
         type = "upper",
         order = "hclust",
         tl.col = "black", tl.srt = 45,
         diag = FALSE)

```

# Species Density

We can use the number of images and counts of independent detections to estimate the density of each species at each camera. Density is the number of animals per unit area, which we express below as the number of animals per square kilometer (km<sup>2</sup>).

To estimate the density of each wildlife species at each camera, we used the Time in Front of Camera (TIFC) approach (@becker2022applying). This method has previously been used to estimate densities of unmarked populations of both White-tailed Deer and Moose in the boreal region of Alberta (@laurent2021evaluating, @dickie2024habitat). Similar to quadrat sampling, this approach involves (for each camera) multiplying the number of animals observed by the total time they spend in front of the camera, which is then divided by the product of the area sampled by the camera and the total time the camera was operating. This calculation yields an estimate of animals per unit area (i.e., 'density').

The following map displays the spatial distribution in densities of the 6 focal species between the 40 cameras of the `r com_acr` camera project. Note that cameras with no detections of a particular species (i.e., a density of '0') are represented with a black circle. Smaller circles represent small densities of a species at that camera. Bigger circles indicate that the density is higher at that camera.

<br>

::: {.callout-tip collapse="false" style="background-color: #f4f4f4; padding: 5px;"}
You can hover over each camera location to see the calculated density of each species at that deployment.
:::

```{r}
#| label: map-species-density
#| include: true
#| echo: false
#| results: asis
#| eval: true
#| warning: false
#| message: false

densities_to_map <- locations_for_map |>
  left_join(df_density_sum) |>
  select(-project) |>
  mutate(density_km2 = ifelse(density_km2 > 20, 20, density_km2)) |>
  mutate(log_density = log10(density_km2 + 1))

buffer <- 0.05

# Create base map
leaf_map <- leaflet(width = "90%", height = "750px") |>
  addProviderTiles("Esri.WorldGrayCanvas", group = "Gray Canvas") |>
  addProviderTiles("Esri.WorldImagery", group = "Satellite Imagery") |>
  fitBounds(
    lng1 = min(densities_to_map$longitude) - buffer,
    lat1 = min(densities_to_map$latitude) - buffer,
    lng2 = max(densities_to_map$longitude) + buffer,
    lat2 = max(densities_to_map$latitude) + buffer
  ) |>
  addFullscreenControl() |>
  addResetMapButton()

max_log <- max(densities_to_map$log_density, na.rm = TRUE)

# Loop over species and add circle markers for each base group
for (sp in species) {
  
  data <- densities_to_map |>
  mutate(species_common_name = ifelse(species_common_name == "Horse", "Feral Horse", species_common_name)) |>
  filter(species_common_name == sp) |>
  mutate(
    scaled_radius = pmax(log_density / max_log * 15, 4),
    fill_color = ifelse(density_km2 == 0, "#222222", species_colours[[sp]]),
    label_text = ifelse(
      density_km2 == 0,
      paste0(
        "<table>",
        "<tr><td>Camera location:</td><td align='right'><b>", location, "</b></td></tr>",
        "<tr><td>Grid:</td><td align='right'><b>", grid, "</b></td></tr>",
        "<tr><td colspan='2'><i>No detections at this location</i></td></tr>",
        "</table>"
      ),
      paste0(
        "<table>",
        "<tr><td>Camera location:</td><td align='right'><b>", location, "</b></td></tr>",
        "<tr><td>Grid:</td><td align='right'><b>", grid, "</b></td></tr>",
        "<tr><td>", species_common_name, " Density:</td><td align='right'><b>",
        round(density_km2, 2), " per km²</b></td></tr>",
        "</table>"
      )
    )
  )
  
  leaf_map <- leaf_map |>
    addCircleMarkers(
      data = data,
      lng = ~longitude,
      lat = ~latitude,
      radius = ~scaled_radius,
      fillColor = ~fill_color,
      fillOpacity = 0.85,
      stroke = TRUE,
      color = "#FFFFFF",
      weight = 1,
      group = sp,
      label = ~ lapply(label_text, HTML),
      labelOptions = labelOptions(
        html = TRUE,
        direction = "top",
        textsize = "14px",
        style = list(
          "padding" = "6px"
        )
      )
    )
}

leaf_map <- leaf_map |>
  addLayersControl(
    baseGroups = species,
    overlayGroups = "Satellite Imagery",
    options = layersControlOptions(collapsed = FALSE),
    position = "topright"
  ) |>
  hideGroup("Satellite Imagery")

tags$div(
  style = "display: flex; justify-content: center;",
  leaf_map)

```

<br>

# Comparisons Between Grids

We can use the density values from the individual cameras to calculate an **average** density value for all the cameras within each grid (Goose Lake and Rubellite West). There were 21 cameras deployed in Goose Lake and 19 in Rubellite West. Each camera can be thought of as a sample of the larger landscape that we're interested in (i.e., the area of the grid) so averaging together the camera values can give us an estimate of the density of the species in that area. We can then compare these average densities between grids (as well as before and after potential development) to get a sense of which area has more animals.

Figure 6 below displays the average density value for each species for both grids. Note that the y-axis (left) values differ for each species, depending on their calculated densities.

<br>

```{r}
#| label: fig-grid
#| include: true
#| echo: false
#| results: asis
#| eval: true
#| warning: false
#| message: false

species <- c(
  "White-tailed Deer", 
  "Black Bear", 
  "Moose", 
  "Coyote", 
  "Snowshoe Hare",
  "Horse")

file_names <- paste0(species, " Grid Comparison.png")

figure_letters <- letters[1:length(species)]

cat("::: panel-tabset\n\n")

for (i in seq_along(species)) {
  title <- species[i]
  letter <- figure_letters[i]
  img_path <- here::here("Figures/BLMS", file_names[i])
  rel_path <- fs::path_rel(img_path, start = getwd())

  cat("### ", title, " {.unnumbered}\n\n", sep = "")
  
  # Output raw HTML as a single string
  html <- glue::glue(
    '<div style="text-align: center;">
       <img src="{rel_path}" style="width:65%;">
       <figcaption style="text-align: center; font-size: 0.95em; margin-top: 0.5em;">
         <strong>Figure 6{letter}.</strong> Density of <strong>{title}</strong> in each of the two grids.
       </figcaption>
     </div>\n\n'
  )
  
  cat(html)
}

cat(":::\n")


```

-   **White-tailed deer** were more abundant in the Rubellite West grid, with a density over double that of the Goose Lake grid (6.5 to 3 animals per km<sup>2</sup>).
-   **Black Bear** had similar densities between the two grids, however Goose Lake was slightly higher.
-   **Moose** were much more abundant in the Rubellite West grid (\~4x more). This area had a significant amount of high quality habitat, capable of supporting many animals.
-   Like black bear, **Coyote** and **Snowshoe Hare** were more abundant in the Goose Lake grid.
-   **Feral horses** were only found in the Goose Lake grid.

<br>

# Where We Expect to Find Species

The ABMI compiles remote camera data collected in Alberta’s boreal region (including the Oil Sands Region) and uses it to develop habitat models for many species. These models incorporate species’ responses to land cover (e.g., vegetation and forest types), human disturbance, and climate variables (seasonal weather patterns).

The maps below show where the models predict each species is most likely to be found both in the `r com_acr` settlement as well as broader area of interest. The area is divided into 1 km<sup>2</sup> pixels, and for each pixel, the model estimates the probability of each species being there based on the habitat and disturbance present in that pixel.

<br>

::: {.callout-tip collapse="false" style="background-color: #f4f4f4; padding: 5px;"}
Use the buttons on the top right of the map to toggle through each species' predictions. You can also turn on **Satellite Imagery** and use the **NONE (Blank)** button to view the imagery underneath the predictions, as well as use the **Camera Locations** button to see where `r com_acr` camera locations are.
:::

```{r}
#| label: map-species-predictions
#| include: true
#| echo: false
#| results: asis
#| eval: true
#| warning: false
#| message: false

pred_species <- species[species != "Horse"]

# Load AOI
#aoi <- st_read(paste0(g_drive_cbme, "BLMS/AOI/BLMS AOI Large.shp"), quiet = TRUE) |>
#  st_transform(4326)

# Load BLMS boundary
blms_boundary <- st_read(paste0(g_drive_cbme, "BLMS/AOI/Buffalo-Lake-Metis-Settlement.shp"),
                         quiet = TRUE) |>
  st_transform(4326)

# Load rasters for each species
raster_list <- pred_species |>
  set_names() |>
  map(~ terra::rast(paste0(g_drive_cbme, "BLMS/Predictions/", .x, " BLMS.tif")))

raster_list_clamped <- map(raster_list, ~ terra::clamp(.x, lower = 0, upper = 0.80, values = TRUE))

domain_raw <- c(0, 0.801)

pal <- colorNumeric(
  palette = colorRampPalette(c("navy", "skyblue", "gold", "red"))(100),
  domain = domain_raw,
  na.color = "transparent"
)

# Start building the leaflet map
map <- leaflet(width = "90%", height = "750px") |>
  addMapPane(name = "Basemap", zIndex = 410) |>
  addMapPane(name = "Satellite Imagery", zIndex = 420) |>
  addMapPane(name = "Species Rasters", zIndex = 430) |>
  addMapPane(name = "AOI Boundary", zIndex = 440) |>
  #addMapPane(name = "Camera Locations", zIndex = 450) |>
  addTiles(group = "Basemap", options = tileOptions(opacity = 1, pane = "Basemap")) |>
  addProviderTiles("Esri.WorldImagery", group = "Satellite Imagery", options = leafletOptions(pane = "Satellite Imagery")) |>
  addFullscreenControl() |>
  addResetMapButton() |>
  addScaleBar(position = "bottomright", options = scaleBarOptions(imperial = FALSE)) |>
  addMiniMap(position = "bottomright") |>
  addPolylines(data = blms_boundary, color = "#0a0909", weight = 1.5, smoothFactor = 0.2, opacity = 1.0, fill = FALSE, options = leafletOptions(pane = "AOI Boundary"))

for (sp in pred_species) {
  map <- map |>
    addRasterImage(
      raster_list_clamped[[sp]],
      colors = pal,
      opacity = 0.8,
      group = sp,
      options = leafletOptions(pane = "Species Rasters")
    )
}

map <- map |>
  addTiles(group = "NONE (Blank)", options = tileOptions(opacity = 0)) |>
  addAwesomeMarkers(data = st_as_sf(locations_for_map, coords = c("longitude", "latitude"), crs = 4326),
                    icon = makeAwesomeIcon(icon = "camera", iconColor = "black", library = "ion", markerColor = "white"),
      group = "Camera Locations",
      popup = paste(
        "<b>", "Location: ", "</b>", locations_for_map$location, "<br><br>",
        "<b>", "Grid:", "</b>", locations_for_map$grid,
        "<br><br>",
        "<b>", "Deployed:", "</b>", locations_for_map$start_date, "<br><br>",
        "<b>", "Refreshed:", "</b>", locations_for_map$end_date, "<br><br>")) |>
  addLegend(
    pal = pal,
    values = domain_raw,
    opacity = 1,
    title = "Probability of<br>Species Presence:",
    position = "bottomright",
    labFormat = labelFormat(
      transform = function(x) x * 100,
      digits = 0,
      suffix = "%"
    )
  ) |>
  addLayersControl(
    overlayGroups = c("Satellite Imagery",
                      "Camera Locations"),
    baseGroups = c(pred_species, "NONE (Blank)"),
    options = layersControlOptions(collapsed = FALSE)
  ) |>
  hideGroup(c("Satellite Imagery", "Camera Locations"))

# Print map

htmltools::tags$div(
  style = "display: flex; justify-content: center;",
  map)

```

<br>

-   **White-tailed Deer** have a high probability of occurrence throughout the `r com_acr` as well as in the surrounding areas. This is especially the case in the southern regions, where a slightly warmer climate and greater human disturbance (especially agriculture) favour the species.
-   **Black Bears** are also predicted to occur throughout the territory, with a higher probability of occurrence through the forested areas. Agricultural regions to both the south and the north limit the species' predicted probability of occurrence.
-   **Moose** have a similar pattern, with higher probabilities of occurrences in the forested and less disturbed areas.
-   **Coyotes** show the opposite pattern, favouring the agricultural areas around the edges of the BLMS territory.
-   **Snowshoe Hare** are predicted to occur in the less disturbed forested areas.

# Recommendations and Next Steps

(This section is available to record the direction provided by the Community to help guide the next phases of the `r com_acr` wildlife monitoring program.)

# References
